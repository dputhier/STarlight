---
author: "D. Puthier & Jessica Chevalier."
date: "`r Sys.Date()`"
title: "Guided tutorial"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteEngine{knitr::knitr}
  %\VignetteIndexEntry{Guided tutorial}
  %\usepackage[UTF-8]{inputenc}
---

# Guided tutorial



## The dataset

Some blabla


# Visualization


# Verbosity level

How it works...

stcompr

```{r verbosity_level, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
library(stcompr)
library(ggplot2)
library(patchwork)
stcompr::set_verbosity(3)
```

# Loading dataset

Some text about the `load_spatial()` function.

```{r load_dataset, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
st_obj_1 <- load_spatial("~/vizgen_input_region_1.txt")
```

# The STGrid object

## slot names, methods...

Objective, what it is...

```{r show, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
st_obj_1
```
```{r}
summary(st_obj_1)
```

```{r slots, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
slotNames(st_obj_1)
```

```{r show_methods, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
stcompr::show_methods()
```

## Subsetting

### Using bin_x / bin_y

```{r binx_1, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
head(bin_x(st_obj_1))
```

```{r binx_2, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
head(bin_y(st_obj_1))
```

```{r binx_3, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
st_obj_1[bin_x(st_obj_1)[50:100], ]
```

### Using gene

```{r genes, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
st_obj_1[c("Rag1", "Cd3e"), ]
```

Delete all Blanks...

```{r delete, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
st_obj_1 <- rm_controls(st_obj_1)
```

### Using numeric

According to the position in gene_names()

```{r blanks, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
st_obj_1[gene_names(st_obj_1)[1:10], ]
```

The n-th genes...

```{r index, echo = TRUE, eval=TRUE, warning=FALSE, results='hide'}
nb_genes(st_obj_1)
st_obj_1[10:12, ]
```

# Image Visualization

Something about the `spatial_image()` function.

* Single gene

```{r single_gene, warning=FALSE, results='hide', echo = FALSE, eval=TRUE}
spatial_image(st_obj_1, gene="Rag1")
```

```{r}
spatial_image(re_bin(st_obj_1["Rag1"], bin_size = 10), gene="Rag1")
```

* sum of counts...

```{r sumcts, warning=FALSE, results='hide', echo = FALSE, eval=TRUE}
spatial_image(st_obj_1, gene="all_genes", scale=FALSE, saturation = 0)
```

## Zooming...

```{r}
st_obj_1_b5 <- re_bin(st_obj_1[c("Rag1", "Ccl19", "Cd19", "Dntt"),], bin_size=5)

```

```{r}
st_obj_1_b5 <- re_bin(st_obj_1, bin_size=5)

spatial_image(st_obj_1_b5, 
             gene="all_genes", 
             overlay_gene = "Cd19", 
             grid=TRUE,
             grid_by = 50)

sub <- st_obj_1_b5[bin_x(st_obj_1_b5)[350:550],
                          bin_y(st_obj_1_b5)[625:875]]

# Could also add Lifr, Ltbr
p1 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Selp", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Selp") + 
  theme(legend.position = "none")

p2 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "S100a6", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("S100a6") + 
  theme(legend.position = "none")

p3 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Dcn", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Dcn") + 
  theme(legend.position = "none")

p4 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Sparc", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Sparc") + 
  theme(legend.position = "none")

p5 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Pecam1", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Pecam1") + 
  theme(legend.position = "none")

p6 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Pdgfra", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Pdgfra") + 
  theme(legend.position = "none")


p7 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Cdh5", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Cdh5") + 
  theme(legend.position = "none")

p8 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Cd36", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Cd36") + 
  theme(legend.position = "none")

p9 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccl19", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccl19") + 
  theme(legend.position = "none")

p10 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Lifr", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Lifr") + 
  theme(legend.position = "none")

(p1 | p2 | p3 | p4 | p9) / (p5 | p6 | p7 | p8 | p10)


p1 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Cxcl12", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Cxcl12") + 
  theme(legend.position = "none")

p2 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Prss16", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Prss16") + 
  theme(legend.position = "none")

p3 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccl25", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccl25") + 
  theme(legend.position = "none")

p4 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Psmb11", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Psmb11") + 
  theme(legend.position = "none")

(p1 | p2) / (p3 | p4 )


p1 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccl22", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccl22") + 
  theme(legend.position = "none")

p2 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Krt14", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Krt14") + 
  theme(legend.position = "none")

p3 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Aire", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Aire") + 
  theme(legend.position = "none")

p4 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Fezf2", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Fezf2") + 
  theme(legend.position = "none")


p5 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccr7", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccr7") + 
  theme(legend.position = "none")

p6 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Igfbp4", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Igfbp4") + 
  theme(legend.position = "none")

(p1 | p2 | p3 ) / (p4 | p5 | p6)



```

```{r}
st_obj_2_b5 <- re_bin(st_obj_2, bin_size=5)

spatial_image(st_obj_2_b5, 
             gene="all_genes", 
             overlay_gene = "Cd19", 
             grid=TRUE,
             grid_by = 50)

sub <- st_obj_2_b5[bin_x(st_obj_2_b5)[101:501],
                          bin_y(st_obj_2_b5)[101:851]]

# Could also add Lifr, Ltbr
p1 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Selp", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Selp") + 
  theme(legend.position = "none")

p2 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "S100a6", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("S100a6") + 
  theme(legend.position = "none")

p3 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Dcn", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Dcn") + 
  theme(legend.position = "none")

p4 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Sparc", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Sparc") + 
  theme(legend.position = "none")

p5 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Pecam1", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Pecam1") + 
  theme(legend.position = "none")

p6 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Pdgfra", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Pdgfra") + 
  theme(legend.position = "none")


p7 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Cdh5", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Cdh5") + 
  theme(legend.position = "none")

p8 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Cd36", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Cd36") + 
  theme(legend.position = "none")

p9 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccl19", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccl19") + 
  theme(legend.position = "none")

p10 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Lifr", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Lifr") + 
  theme(legend.position = "none")

(p1 | p2 | p3 | p4 | p9) / (p5 | p6 | p7 | p8 | p10)


p1 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Cxcl12", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Cxcl12") + 
  theme(legend.position = "none")

p2 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Prss16", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Prss16") + 
  theme(legend.position = "none")

p3 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccl25", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccl25") + 
  theme(legend.position = "none")

p4 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Psmb11", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Psmb11") + 
  theme(legend.position = "none")

(p1 | p2) / (p3 | p4 )


p1 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccl22", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccl22") + 
  theme(legend.position = "none")

p2 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Krt14", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Krt14") + 
  theme(legend.position = "none")

p3 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Aire", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Aire") + 
  theme(legend.position = "none")

p4 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Fezf2", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Fezf2") + 
  theme(legend.position = "none")


p5 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Ccr7", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Ccr7") + 
  theme(legend.position = "none")

p6 <-  spatial_image(sub, 
             gene="all_genes", 
             overlay_gene = "Igfbp4", 
             colors_overlay = c("green", "red"), 
             size=0.3) + ggtitle("Igfbp4") + 
  theme(legend.position = "none")

(p1 | p2 | p3 ) / (p4 | p5 | p6)



```

```{r loadlib1, warning=FALSE, results='hide', echo = FALSE, eval=TRUE}

spatial_image(st_obj_1_b10[bin_x(st_obj_1_b10)[100:400], 
                           bin_y(st_obj_1_b10)[100:400]], 
              gene="Cd3e")
```

# Comparing counts

## Creating an STCompR object

```{r}
st_obj_2 <- load_spatial("~/vizgen_input_region_0.txt")
st_obj_2 <- rm_controls(st_obj_2)
```
```{r}
st_obj_2
```

```{r}
cmp <- stcompr(rm_controls(st_obj_1), rm_controls(st_obj_2), 
               name_1 = "young", name_2 = "old")
```


```{r}
cmp_volcano(cmp)
```



## Visualizing counts

### Barplot

```{r}
cmp_bar_plot(cmp, transform = "log10", normalized = FALSE)
```

### Boxplot

```{r}
cmp_boxplot(cmp, transform = "log10", normalized = TRUE)
```

# Density of molecules

## Ripley's K function

### Computing Ripley's K function

```{r}
st_obj_1 <- compute_k_ripley(st_obj_1)
st_obj_2 <- compute_k_ripley(st_obj_2)
```

```{r}
head(ripley_k_function(st_obj_1))
```

```{r}
plot_rip_k(st_obj_1) +  plot_rip_k(st_obj_2) 
```

### Comparing density across samples

```{r}
spatial_image(st_obj_1, gene="Cd36")
```
```{r}
spatial_image(st_obj_1, gene="Ighd") + ggtitle("Young") + spatial_image(st_obj_2, gene="Ighd") + ggtitle("Old") 
```
```{r}
spatial_image(st_obj_1, gene="Cd19") + ggtitle("Young") + spatial_image(st_obj_2, gene="Cd19") + ggtitle("Old") 
```

# The strange case of Blanks...


```{r}
goi <- unique(grep("^Blank-[0-9]", 
                   st_obj_1@coord$gene, val=TRUE))
ggplot(st_obj_1@coord[st_obj_1@coord$gene %in% goi,],
       mapping=aes(x=x, y=y, col=gene)) +
  geom_point(size=0.5, shape=15) + theme_bw() +
ggplot(st_obj_2@coord[st_obj_2@coord$gene %in% goi,],
       mapping=aes(x=x, y=y, col=gene)) +
  geom_point(size=0.5, shape=15) + theme_bw() 
```

```{r}
ggplot(st_obj_1@coord[st_obj_1@coord$gene %in% goi,],
       mapping=aes(x=x, y=y, col=gene)) +
  geom_point(size=0.5, shape=15) + theme_bw()
```


```{r}
spatial_image(st_obj_1, 
              gene = "all_genes", 
              overlay_gene = "Cd19", 
              colors = c("#000000", "#0f9b0f"), 
              colors_overlay = c("#DEEBF7", 
                                 "#9ECAE1", 
                                 "#3182BD", 
                                 "#0000FF"))
```

# Subset 

```{r}
spatial_image(st_obj_2[bin_x(st_obj_2)[50:70], bin_y(st_obj_2)[120:150]], gene="Ighd")
bin_x(st_obj_2)[50:70]
bin_y(st_obj_2)[120:150]
```

## Neighborhing changes

```{r}
heatmap_nbh_changes(cmp, what ="changes", only_genes = c("Ccl19", "Ccl25", "Rag1", "Dntt", "Pcna", "Mki67", "Ccl22", "Spib", "Cd19", "Ighd", "Cd3e", "Cd3d", "Ccr6", "Dcn", "Ccr9", "Cxcl12", "Aire", "fezf2", "Krt14", "H2-Aa", "S1pr1"), hclust_method = "single")
```

## Session info

```{r}
sessionInfo()
```

